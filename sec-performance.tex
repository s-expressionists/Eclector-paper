\section{Performance}
\label{sec-performance}

\todo{capitalization section vs Listing}
As stated in \refsec{sec-eclector}, one of the goals for \eclector{}
was to achieve \emph{acceptable} performance.

\subsection{Benchmarks}
\label{sec:benchmarks}

We measured the time it took the native readers of different \cl{}
implementations as well as extrinsic \eclector{} in those same
implementation to read the contents of \cl{} files taken from
real-world open source libraries.  Code similar to the following was
used to perform the benchmarking:

\begin{listing}[H]
\begin{minted}[fontsize=\small,linenos,escapeinside=||]{lisp}
(let ((content (alexandria:read-file-into-string filename)))
  #+sbcl (sb-ext:gc :full t)
  #+ccl  (gc)
  (the-cost-of-nothing:benchmark
   (with-input-from-string (stream content)
     (loop for form = (|\guillemotleft|read|\guillemotright| stream nil stream)
           until (eq form stream)))))
\end{minted}
\end{listing}

where \texttt{\guillemotleft{}read\guillemotright{}} is either
\texttt{cl:read}, \texttt{eclector.reader:read} or
\texttt{eclector.concrete-syntax-tree:read} depending on the
benchmark.

\todo{eclector version}
\todo{describe hardware}
\todo{more lisps?}

We performed the benchmark described above for the following \cl{}
implementations:

\todo{optimization settings}
\begin{tabular}{ll}
  \textbf{Implementation} & \textbf{Version}\\
  \sbcl{}                 & 2.6.0.debian\\
  Clozure Common Lisp     & 1.13 (v1.13-21-gda7138ef) LinuxX8664\\
\end{tabular}

Unfortunately, results for ECL are missing since the ECL version
packaged for the operating system of the authors has a bug that
prevents it form executing \eclector{}%
\footnote{\url{https://gitlab.com/embeddable-common-lisp/ecl/-/issues/802,
    issue 802}}.  Building ECL from source failed due to issues with
the default C compiler on the author's system.

We arbitrarily selected the following large \cl{} files as inputs for
the benchmark runs:

\begin{tabular}{llr}
  \textbf{File}   & \textbf{Library}           & \textbf{Lines of Code}\\
  enc-cn-tbl.lisp & flexi-streams \todo{version} & 48,418\\
  screamer.lisp   & screamer \todo{version}      & 7,233
\end{tabular}

Results

\begin{tabular}{lrr}
  \textbf{Reader}         & \textbf{enc-cn-tbl.lisp} & \textbf{screamer.lisp}\\
  \sbcl{} native          & 0.034 s                  & 0.011 s\\
  \sbcl{} \eclector{}     & 0.161 s                  & 0.052 s\\
  \sbcl{} \eclector{} CST & 0.475 s                  & 0.096 s\\
  \ccl{} native           & 0.186 s                  & 0.047 s\\
  \ccl{} \eclector{}      & 1.249 s                  & 0.607 s\\
  \ccl{} \eclector{} CST  & 4.108 s                  & 0.961 s\\
\end{tabular}

Observations
\begin{itemize}
\item for the chosen inputs, \sbcl{} runs \eclector{} around 7 to 10 times faster than \ccl{}
\item \eclector{} on \sbcl{} is on par with \ccl{}
\item Data inputs \texttt{enc-cn-tbl.lisp} two orders of magnitude,
  Code inputs \texttt{screamer.lisp} one order of magnitude
\end{itemize}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: "eclector"
%%% End:
